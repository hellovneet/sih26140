"use client";
import {useState} from "react"; import AppShell from "@/components/layout/AppShell"; import Topbar from "@/components/layout/Topbar";
export default function Quiz(){const [a,setA]=useState("");return <AppShell><Topbar title="AI Quiz"/><div className="mx-auto max-w-3xl"><div className="glass p-7"><span className="text-xs muted">QUESTION 1 / 5</span><h2 className="mt-6 text-2xl font-bold">What does a Hadamard gate create from |0⟩?</h2><div className="mt-7 grid gap-3">{[["A","Superposition"],["B","Classical bit"],["C","Encryption key"],["D","Measurement collapse"]].map(([k,v])=><button key={k} onClick={()=>setA(k)} className={`rounded-xl border p-4 text-left ${a===k?"border-cyan-300 bg-[#102b3d]":"border-[#1d3552]"}`}><b>{k}.</b> {v}</button>)}</div>{a&&<p className="mt-5 text-sm">{a==="A"?"Correct 🎯 Hadamard creates equal superposition.":"Not quite. Review Quantum Gates and try again."}</p>}</div></div></AppShell>}